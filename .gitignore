# forester build output (published to docs/ by CI, or commit.sh locally)
output/

# local opam switch (created by CI's setup-ocaml, occasionally locally)
_opam/

# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
